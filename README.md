# Q Tree Model

Small test to see if I can extract the Q learning portion out of HRM

## Commands

Run the main model

```sh
uv run main.py
```

Run tests

```sh
uv run pytest
```

Run type checks

```sh
uv run basedpyright
```
